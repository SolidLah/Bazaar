import useSWR from "swr";

const ListForm = () => {
/*
  cases
    - already a market item, but is not active and no price
    - not a market item
*/

  const { data } = useSWR();

  return <div></div>;
};

export default ListForm;
